import { useQuery } from '@tanstack/react-query'
import { ArrowUpRight } from 'lucide-react'
import { Link } from 'react-router-dom'
import { EmptyState, LoadingState } from '../components/LoadingState'
import { fetchPublishedNews } from '../lib/data'
import { formatDate } from '../lib/format'

export function NewsPage(){const q=useQuery({queryKey:['news'],queryFn:()=>fetchPublishedNews(),retry:false});return <main className="px-5 py-16 md:px-8 md:py-24"><div className="mx-auto max-w-[1240px]"><div className="max-w-3xl"><div className="text-xs font-bold uppercase tracking-[.18em] text-brand-500">Aktuality</div><h1 className="mt-3 text-5xl font-black tracking-[-.06em] text-brand-900 sm:text-6xl">Co se děje v klubu.</h1></div><div className="mt-12">{q.isLoading?<LoadingState rows={5}/>:q.data?.length?<div className="grid gap-5 md:grid-cols-2 lg:grid-cols-3">{q.data.map((a)=><Link key={a.id} to={`/aktuality/${a.slug}`} className="group overflow-hidden rounded-5xl border border-sand-200 bg-white transition hover:-translate-y-1 hover:shadow-soft"><div className="aspect-[16/10] bg-sand-100">{a.cover_image?<img src={a.cover_image} alt="" className="h-full w-full object-cover transition duration-500 group-hover:scale-[1.02]"/>:<div className="h-full bg-[radial-gradient(circle_at_30%_20%,rgba(0,146,63,.2),transparent_32%)]"/>}</div><div className="p-6"><div className="flex items-center justify-between"><span className="text-[10px] font-bold uppercase tracking-[.16em] text-brand-500">{a.category||'Aktualita'}</span><ArrowUpRight size={17} className="text-ink-500"/></div><h2 className="mt-3 text-2xl font-extrabold tracking-[-.04em] text-brand-900">{a.title}</h2>{a.excerpt&&<p className="mt-3 line-clamp-3 text-sm leading-6 text-ink-500">{a.excerpt}</p>}<div className="mt-5 text-xs text-ink-500">{formatDate(a.published_at)}</div></div></Link>)}</div>:<EmptyState title="Zatím bez aktualit" text="Po zveřejnění prvního článku se objeví zde."/>}</div></div></main>}
